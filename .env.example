# Same Supabase project used by Plekxa Enterprise OS and Plekxa Studio
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Required because the existing CMS tables are protected by admin-only RLS.
# Add this only in Vercel/server environment. Never expose it with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=
