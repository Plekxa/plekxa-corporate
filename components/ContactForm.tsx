"use client";
import { FormEvent, useState } from "react";

export function ContactForm(){
 const [state,setState]=useState<"idle"|"sending"|"sent"|"error">("idle");
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("sending");const form=new FormData(e.currentTarget);const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key){setState("error");return;}const category=String(form.get("category")||"general");const payload={name:String(form.get("name")||""),email:String(form.get("email")||""),topic:String(form.get("topic")||category),category,message:String(form.get("message")||""),status:"new"};try{const response=await fetch(`${url}/rest/v1/support_requests`,{method:"POST",headers:{apikey:key,Authorization:`Bearer ${key}`,"Content-Type":"application/json",Prefer:"return=minimal"},body:JSON.stringify(payload)});if(!response.ok)throw new Error();setState("sent");e.currentTarget.reset();}catch{setState("error");}}
 return <form className="contact-form" onSubmit={submit}><div className="form-row"><label>Name<input name="name" required autoComplete="name"/></label><label>Email<input type="email" name="email" required autoComplete="email"/></label></div><label>What can we help with?<select name="category" defaultValue="general"><option value="general">General enquiry</option><option value="creator">Creator partnership</option><option value="business">Business partnership</option><option value="press">Press and media</option><option value="careers">Careers</option><option value="legal">Legal</option></select></label><label>Subject<input name="topic" required/></label><label>Message<textarea name="message" rows={7} required/></label><button className="pill-button pill-button--dark" type="submit" disabled={state==="sending"}>{state==="sending"?"Sending…":"Send enquiry"}</button>{state==="sent"&&<p className="form-success">Thank you. Your enquiry has been sent to the Plekxa support centre.</p>}{state==="error"&&<p className="form-error">We could not send your message. Please email hello@plekxa.com.</p>}</form>
}
